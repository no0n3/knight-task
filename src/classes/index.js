const { Point } = require('./point');
const { Knight } = require('./knight');
const { MoveType } = require('./move-type')
const { MoveMapper } = require('./move-mapper')

module.exports = {
  Point, Knight, MoveType, MoveMapper
};
